import SectionTitle from "../../components/SectionTitle";

const OurMenueSection = () => {
    return (
        <div>
            <SectionTitle title={`from our menue `} subTitle={'Check it out'}></SectionTitle>
            
        </div>
    );
};

export default OurMenueSection;